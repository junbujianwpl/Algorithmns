
* 最大子数组。  
暴力法。
分治法。
前缀和，并找出到当前前缀和的最小值，同时记录到当前索引前缀和减前缀和最小值的差值，并记录出最小的差值。时间O(n)。
参考最近0和子数组算法，算出前缀和后排序，用最大减最小。O(nlogn)

* 有序数组旋转后查找分界点最小元素的二分查找法。
* 最近0和子数组 前缀和排序相邻相减，若求最大，排序后数组尾首相减
若参考最大子数组，有O(n)的算法
* Longest Commom Sequence。动态规划，从最后一个元素相等与不等得出递推公式，然后分析初始值。打表计算即可。
变种引申问题，
Largest Increase Sequence。排序后求二者LCS。
Longest Commom Substring。初值不同而矣。定义的S(i)含义为包含i元素的前缀最长子串长度。然后分析递推公式。
编辑距离。打表初值为递增序列。

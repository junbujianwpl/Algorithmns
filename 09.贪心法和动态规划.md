
1. 认识论
	1. 认识事物的方法：概念，判断，推理
	2. 推理中又分为归纳、演绎。
	3. 重点考查归纳推理的具体方法。
	4. 形式化表述。
		1. 已知：问题规模为n的前提A
		2. 求解，未知结论B
		3. 记号：A_n表示“问题规模为n的已知条件”
		
2. 对归纳推理的理解
	1. A0->A1,A(n-1)->A(n)。数学归纳法。
	2. {A(0)}->{A(1)},{A(0),A(1)}->A(2),...{A(0)..A(n-1)}->A(n)。
	
3. 实例问题
	1. LIS算法。Longest Increasing Subarray。
		1. 
		
	2. 矩阵连乘加括号
	
	
	3. 字符串交替连接
		1. 无重复，归并的思路。
		2. 有重复，用压栈的方法
		3. 用动态规划解决
			1. dp[i,j]表示s3[1,i+j]是否由s1[1,i]和s2[1,j]字符组成。即取值为true/false
			2. 如果s1[i]==s3[i+j],且dp[i-1,j]为真，那么dp[i][j]为真
			3. 如果s2[j]==s3[i+j],且dp[i,j-1]为真，那么dp[i][j]为真
			4. 其它情况为假
			
	4. 走棋盘/格子取数
		1. 走完，权值和最小。
		2. dp[0,0]=a[0,0]
		3. dp[x,y]=min(dp[x-1,y],dp[x,y-1])
		4. 应用，给定空间两条曲线，设计经过这两条曲线的曲面，使得该曲面是“最合理的”。
	5. 往返的走格子，要求权值和最小
		1. dp(i,j,k)。i表示第几步，j表示第一次取的数，k表示第二次走取的数。
		
	6. 卡塔兰数
	